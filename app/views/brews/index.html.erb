<p style="color: green"><%= notice %></p>

<% content_for :title, "Brews" %>

<h1>Brews</h1>

<%= link_to "New brew", new_brew_path %>
<br>
<br>

<div id="brews">
  <% @brews.each do |brew| %>
    <%= render brew %>
    <p>
      <%= link_to "Show this brew", brew %>
    </p>
    <br>
  <% end %>
</div>
